
import 'import_generic_function.dart';

void main() {
  var numbers = [1, 2, 3, 4, 5, 6];
  var names = ["Samman", "Mansa", "Shira"];

  print(ArrayHelper.count(numbers));
  print(ArrayHelper.count(names));
}